#Specification Parttner

É um  padrão de design de software, onde as regras de negócio podem ser recombinadas através de logicas booleanas.

Este padrão permite encapsular algumas regras de domínio em uma única uniddae(especificação) e reutiliza-la em diferentes partes.